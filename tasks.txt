Create a `pyproject.toml` file in the root directory. Define the project name as "smartwaste", require Python >=3.10, and list dependencies: opencv-python, depthai, pandas, google-genai, openpyxl, pytest, pytest-cov, ruff, mypy, pydantic, pydantic-settings, python-dotenv, tenacity. Add basic configuration sections for ruff, mypy, and pytest.
Create a `tests/` directory with an empty `__init__.py` and a `conftest.py`. Write basic pytest unit tests in `tests/test_parser.py` targeting the `_extract_json()` function to ensure it properly handles markdown fences, garbage text prefixes, and basic valid JSON.
Fix the thread safety bug in `state.py`. Ensure `toggle_auto()` acquires `self._lock` before modifying `self.auto_classify` to prevent data races with `get_display()`.
Fix the thread safety bug in `dataset.py`. The `_metadata` list is shared across threads. Protect `save_entry()` and the JSON rewriting logic with a threading lock to prevent corruption during concurrent classifications.
Refactor `classifier.py` to handle the Gemini API key securely. Use `python-dotenv` to load the key from the environment, and initialize the Gemini client lazily inside the classification function rather than at module import time.
Refactor `dataset.py` to stop rewriting the entire Excel file on every single classification. Make the existing SQLite database the sole primary source of truth for saving entries, removing the hot-path Excel and JSON file writes entirely.
Modify `dataset.py`. The `_environment_data()` function generates fake sensor data. Rename these output fields to prefix them explicitly with 'simulated_' (e.g., 'simulated_temperature') so they are clearly marked as fake in all database writes.
Update `classifier.py` to add robust retry logic for the Gemini API call. Use the `tenacity` library to implement an exponential backoff strategy (e.g., 3 attempts) for handling transient network errors.
Create a Pydantic model named `ClassificationResult` to define the expected schema from the Gemini API (category, description, brand_product). Update the JSON parsing logic to validate the API response against this Pydantic model.
Implement a centralized configuration system using `pydantic-settings`. Move the hardcoded variables from `config.py` into this system, and add configurable variables for DEVICE_ID, BIN_ID, and LOCATION (defaulting to 'Yerevan'). Update `dataset.py` to use these new config values.
Refactor `main.py` and `mainauto.py` to eliminate duplicated code. Extract the shared camera initialization, frame capture loop, and cleanup logic into a new `run_loop(strategy)` function in `smartwaste/app.py`. Update both main files to act as thin wrappers calling this new function.
Create a `.github/workflows/ci.yml` file to set up a basic GitHub Actions pipeline. It should check out the code, set up Python, install dependencies via pip, run `ruff check`, `ruff format --check`, `mypy --strict smartwaste/`, and `pytest --cov=smartwaste tests/`.
